class PropWithIf {
    public int foo() {
        int a;
        int b;
        int res;
        a = 5;
        b = 10;
        if (a < b) {
            a = 20; // Modifies a constant
            res = a + b;
        } else {
            b = 30; // Modifies a constant
            res = a - b;
        }
        return res;
    }

    public static void main(String[] args) {
    }
}