import ioPlus;

IfWhileNested {

    .method public static main(args.array.String).V {

        a.i32 :=.i32 10.i32;

        i.i32 :=.i32 5.i32;

        whileCond1:

            t1.bool :=.bool i.i32 <.bool a.i32;

        if (t1.bool) goto whileLoop1;
        goto whileEnd1;

        whileLoop1:

            i.i32 :=.i32 i.i32 +.i32 3.i32;

            invokestatic(ioPlus, "printResult", 1.i32).V;

        goto whileCond1;
        whileEnd1:

        ret.V;

    }

    .construct IfWhileNested().V {
        invokespecial(this, "<init>").V;
    }
}